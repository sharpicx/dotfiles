import { ITextDocument } from '../types/textDocument';
import { MdLinkProvider } from './documentLinks';
import * as lsp from 'vscode-languageserver-types';
import { IWorkspace } from '../workspace';
import { IMdParser } from '../parser';
import type { CancellationToken, CompletionContext } from 'vscode-languageserver-protocol';
import { LsConfiguration } from '../config';
/**
 * Adds path completions in markdown files.
 */
export declare class MdPathCompletionProvider {
    private readonly configuration;
    private readonly workspace;
    private readonly parser;
    private readonly linkProvider;
    constructor(configuration: LsConfiguration, workspace: IWorkspace, parser: IMdParser, linkProvider: MdLinkProvider);
    provideCompletionItems(document: ITextDocument, position: lsp.Position, _context: CompletionContext, _token: CancellationToken): Promise<lsp.CompletionItem[]>;
    private readonly linkStartPattern;
    private readonly referenceLinkStartPattern;
    private readonly definitionPattern;
    private getPathCompletionContext;
    /**
     * Check if {@param ref} looks like a 'http:' style url.
     */
    private refLooksLikeUrl;
    private getAnchorContext;
    private provideReferenceSuggestions;
    private provideHeaderSuggestions;
    private providePathSuggestions;
    private resolveReference;
    private resolvePath;
    private getFileUriOfTextDocument;
}
